#include "main.h"

/**
 * factorial - returns the factorial of a given number
 * @s: int
 * Return: Always 0 success
 */
int factorial(int n)
{
	if (n < 0)
	{
		return (-1);
	}
	else if (m== n)
		return 1;
}
return (n * factorial(n - 1));
}
