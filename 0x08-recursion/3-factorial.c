#include "main.h"

/**
 * factorial - returns the factorial of a given number
 * @n: number whose factoriel is to be returned
 *
 * Return: factorial
 */
int factorial(int n)
{
	if (n < 0)
		return (-1);
	else if (m == n)
		return (1);
return (n * factorial(n - 1));
}
